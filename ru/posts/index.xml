<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Блог on x3ps</title><link>https://x3ps.dev/ru/posts/</link><description>Recent content in Блог on x3ps</description><generator>Hugo -- gohugo.io</generator><language>ru</language><copyright>2025</copyright><lastBuildDate>Thu, 19 Mar 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://x3ps.dev/ru/posts/index.xml" rel="self" type="application/rss+xml"/><item><title>Пишем Go-библиотеку для Reticulum: go-rns-pipe</title><link>https://x3ps.dev/ru/posts/2026/03/writing-a-go-library-for-reticulum-go-rns-pipe/</link><pubDate>Thu, 19 Mar 2026 00:00:00 +0000</pubDate><guid>https://x3ps.dev/ru/posts/2026/03/writing-a-go-library-for-reticulum-go-rns-pipe/</guid><description>&lt;h2 id="что-такое-reticulum-и-зачем-мне-понадобился-go"&gt;Что такое Reticulum и зачем мне понадобился Go&lt;/h2&gt;
&lt;p&gt;&lt;a href="https://reticulum.network/"&gt;Reticulum Network Stack&lt;/a&gt; — это криптографический сетевой стек, спроектированный
для работы в условиях низкой пропускной способности и ненадёжных каналов. Ключевая идея: абстракция над
физическим транспортом. Reticulum умеет работать поверх LoRa, TCP, UDP, последовательных портов — и через
PipeInterface, которому посвящена эта статья.&lt;/p&gt;
&lt;p&gt;PipeInterface позволяет запустить произвольный процесс и общаться с rnsd через stdin/stdout. Демон rnsd
выступает как «сервер», а ваш процесс — как транспорт. Данные передаются в обе стороны HDLC-фреймами.
Эталонная реализация написана на Python (&lt;code&gt;PipeInterface.py&lt;/code&gt;), но мне понадобился Go.&lt;/p&gt;</description><content type="html"><![CDATA[<h2 id="что-такое-reticulum-и-зачем-мне-понадобился-go">Что такое Reticulum и зачем мне понадобился Go</h2>
<p><a href="https://reticulum.network/">Reticulum Network Stack</a> — это криптографический сетевой стек, спроектированный
для работы в условиях низкой пропускной способности и ненадёжных каналов. Ключевая идея: абстракция над
физическим транспортом. Reticulum умеет работать поверх LoRa, TCP, UDP, последовательных портов — и через
PipeInterface, которому посвящена эта статья.</p>
<p>PipeInterface позволяет запустить произвольный процесс и общаться с rnsd через stdin/stdout. Демон rnsd
выступает как «сервер», а ваш процесс — как транспорт. Данные передаются в обе стороны HDLC-фреймами.
Эталонная реализация написана на Python (<code>PipeInterface.py</code>), но мне понадобился Go.</p>
<p>Причина глубже, чем просто «не хочу тащить Python-рантайм» (хотя и это тоже). Задача была сделать
библиотеку <strong>независимой от реализации</strong>: не привязанной к Python, пригодной как строительный блок
для любой реализации Reticulum.</p>
<p>Сейчас Reticulum существует только как эталонная Python-реализация, но активно разрабатываются
реализации на Go и Rust. PipeInterface — это естественный шов: любой язык, умеющий говорить
HDLC поверх stdin/stdout, может подключиться к rnsd без изменений в самом демоне. Именно с этим
будущим в голове проектировался <a href="https://github.com/x3ps/go-rns-pipe">go-rns-pipe</a>.</p>
<h2 id="задача">Задача</h2>
<p>rnsd запускает дочерний процесс командой из конфига:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-ini" data-lang="ini"><span style="display:flex;"><span><span style="color:#66d9ef">[interfaces]</span>
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">[[My Go Transport]]</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">type</span> <span style="color:#f92672">=</span> <span style="color:#e6db74">PipeInterface
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    interface_enabled = Yes
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    command = /usr/local/bin/my-transport
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    respawn_delay = 5</span>
</span></span></code></pre></div><p>Дочерний процесс общается с rnsd через stdin/stdout:</p>
<ul>
<li><strong>stdin</strong> — поток входящих HDLC-фреймов от rnsd (пакеты, которые нужно передать наружу)</li>
<li><strong>stdout</strong> — поток исходящих HDLC-фреймов к rnsd (принятые пакеты)</li>
</ul>
<p>Протокол простой, но нужно точно воспроизвести HDLC-фреймирование из Python — иначе rnsd не поймёт
ни одного пакета.</p>
<h2 id="архитектура">Архитектура</h2>
<p>Библиотека состоит из шести компонентов:</p>
<pre tabindex="0"><code>         stdin
           │
           ▼
      ┌─────────┐    пакеты    ┌──────────────────┐
      │ Decoder │ ──────────►  │                  │
      └─────────┘              │    Interface     │ ◄── OnSend callback
                               │                  │
      ┌─────────┐   фрейм      │  pipeOnline      │ ◄── Start()/readLoop()
      │ Encoder │ ◄──────────  │  transportOnline │ ◄── SetOnline(bool)
      └─────────┘              └──────────────────┘
           │                          │
           ▼                     Reconnector
         stdout                        │
                                  Config / slog
</code></pre><ul>
<li><strong><code>Interface</code></strong> — центральный тип. Оркестрирует чтение/запись, управляет двухбитным состоянием
<code>online</code>, вызывает коллбэки.</li>
<li><strong><code>Encoder</code></strong> — оборачивает произвольный байтовый срез в HDLC-фрейм.</li>
<li><strong><code>Decoder</code></strong> — потоковый автомат состояний. Реализует <code>io.Writer</code>, внутри хранит канал готовых пакетов.</li>
<li><strong><code>Reconnector</code></strong> — управляет логикой повторных подключений: фиксированная задержка или экспоненциальный
бэкофф.</li>
<li><strong><code>Config</code></strong> — конфигурация с разумными дефолтами, совпадающими с <code>PipeInterface.py</code>.</li>
<li><strong><code>SetOnline(bool)</code></strong> — внешний сигнал о состоянии сетевой стороны транспорта.</li>
</ul>
<p>Поток данных при получении пакета от rnsd:</p>
<ol>
<li><code>io.Copy</code> в горутине читает байты из <code>config.Stdin</code> и пишет их в <code>Decoder</code>.</li>
<li><code>Decoder.Write</code> прогоняет каждый байт через автомат состояний.</li>
<li>Готовый пакет кладётся в буферизованный канал <code>packets</code>.</li>
<li>Основной <code>select</code>-цикл в <code>readLoop</code> читает из канала и вызывает <code>OnSend</code>-коллбэк.</li>
</ol>
<p>Отправка пакета к rnsd — <code>iface.Receive(pkt)</code> — кодирует его через <code>Encoder</code> и пишет в <code>config.Stdout</code>.</p>
<h3 id="setonline-двухбитное-состояние">SetOnline: двухбитное состояние</h3>
<p><code>Interface</code> управляет двумя независимыми флагами online:</p>
<ul>
<li><strong><code>pipeOnline</code></strong> — rnsd-сторона жива. Выставляется в <code>true</code> при успешном <code>Start()</code>, в <code>false</code>
при EOF или ошибке <code>readLoop</code>.</li>
<li><strong><code>transportOnline</code></strong> — сетевая сторона жива. Управляется вызовом <code>SetOnline(bool)</code> из транспорта.</li>
</ul>
<p>Эффективное состояние: <code>pipeOnline &amp;&amp; transportOnline</code>. Коллбэк <code>onStatus</code> срабатывает только при
реальных переходах, чтобы rnsd не получал лишних уведомлений.</p>
<p>Типичный сценарий для TCP-транспорта:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#75715e">// TCP-соединение потеряно — сообщаем, что сетевая сторона недоступна</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">func</span> (<span style="color:#a6e22e">t</span> <span style="color:#f92672">*</span><span style="color:#a6e22e">TCPTransport</span>) <span style="color:#a6e22e">onDisconnect</span>() {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">t</span>.<span style="color:#a6e22e">iface</span>.<span style="color:#a6e22e">SetOnline</span>(<span style="color:#66d9ef">false</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// ...логика переподключения...</span>
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">// TCP-соединение восстановлено</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">func</span> (<span style="color:#a6e22e">t</span> <span style="color:#f92672">*</span><span style="color:#a6e22e">TCPTransport</span>) <span style="color:#a6e22e">onConnect</span>() {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">t</span>.<span style="color:#a6e22e">iface</span>.<span style="color:#a6e22e">SetOnline</span>(<span style="color:#66d9ef">true</span>)
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Это позволяет rnsd корректно отображать состояние интерфейса даже если pipe к rnsd жив, но
сетевая сторона временно недоступна.</p>
<h2 id="hdlc-фреймирование">HDLC-фреймирование</h2>
<h3 id="как-устроен-фрейм">Как устроен фрейм</h3>
<p>HDLC (High-Level Data Link Control) в упрощённом виде, похожем на PPP:</p>
<pre tabindex="0"><code>0x7E | escaped_data | 0x7E
</code></pre><p>Байт <code>0x7E</code> — флаг начала/конца фрейма. Если в полезной нагрузке встречается <code>0x7E</code> или <code>0x7D</code>
(escape-символ), их нужно экранировать:</p>
<table>
  <thead>
      <tr>
          <th>Исходный байт</th>
          <th>Замена</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>0x7D</code></td>
          <td><code>0x7D 0x5D</code></td>
      </tr>
      <tr>
          <td><code>0x7E</code></td>
          <td><code>0x7D 0x5E</code></td>
      </tr>
  </tbody>
</table>
<p>Правило: сначала экранируем <code>0x7D</code>, потом <code>0x7E</code>. Порядок важен — иначе дважды заэкранируем.</p>
<h3 id="encoder">Encoder</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#66d9ef">const</span> (
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">HDLCFlag</span>    = <span style="color:#ae81ff">0x7E</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">HDLCEscape</span>  = <span style="color:#ae81ff">0x7D</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">HDLCEscMask</span> = <span style="color:#ae81ff">0x20</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">func</span> (<span style="color:#a6e22e">e</span> <span style="color:#f92672">*</span><span style="color:#a6e22e">Encoder</span>) <span style="color:#a6e22e">Encode</span>(<span style="color:#a6e22e">packet</span> []<span style="color:#66d9ef">byte</span>) []<span style="color:#66d9ef">byte</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">out</span> <span style="color:#f92672">:=</span> make([]<span style="color:#66d9ef">byte</span>, <span style="color:#ae81ff">0</span>, len(<span style="color:#a6e22e">packet</span>)<span style="color:#f92672">+</span>len(<span style="color:#a6e22e">packet</span>)<span style="color:#f92672">/</span><span style="color:#ae81ff">4</span><span style="color:#f92672">+</span><span style="color:#ae81ff">2</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">out</span> = append(<span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">HDLCFlag</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> <span style="color:#a6e22e">_</span>, <span style="color:#a6e22e">b</span> <span style="color:#f92672">:=</span> <span style="color:#66d9ef">range</span> <span style="color:#a6e22e">packet</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">switch</span> <span style="color:#a6e22e">b</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">case</span> <span style="color:#a6e22e">HDLCEscape</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">out</span> = append(<span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">HDLCEscape</span>, <span style="color:#a6e22e">HDLCEscape</span>^<span style="color:#a6e22e">HDLCEscMask</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">case</span> <span style="color:#a6e22e">HDLCFlag</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">out</span> = append(<span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">HDLCEscape</span>, <span style="color:#a6e22e">HDLCFlag</span>^<span style="color:#a6e22e">HDLCEscMask</span>)
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">default</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">out</span> = append(<span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">b</span>)
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">out</span> = append(<span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">HDLCFlag</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">out</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p><code>HDLCEscape ^ HDLCEscMask</code> = <code>0x7D ^ 0x20</code> = <code>0x5D</code>. <code>HDLCFlag ^ HDLCEscMask</code> = <code>0x7E ^ 0x20</code> = <code>0x5E</code>.
Именно эти значения ожидает <code>PipeInterface.py</code>.</p>
<h3 id="decoder-автомат-состояний">Decoder: автомат состояний</h3>
<p>Декодер — потоковый. Он реализует <code>io.Writer</code>, чтобы его можно было передать прямо в <code>io.Copy</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#66d9ef">func</span> (<span style="color:#a6e22e">d</span> <span style="color:#f92672">*</span><span style="color:#a6e22e">Decoder</span>) <span style="color:#a6e22e">Write</span>(<span style="color:#a6e22e">b</span> []<span style="color:#66d9ef">byte</span>) (<span style="color:#66d9ef">int</span>, <span style="color:#66d9ef">error</span>) {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">mu</span>.<span style="color:#a6e22e">Lock</span>()
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">defer</span> <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">mu</span>.<span style="color:#a6e22e">Unlock</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> <span style="color:#a6e22e">_</span>, <span style="color:#a6e22e">byte_</span> <span style="color:#f92672">:=</span> <span style="color:#66d9ef">range</span> <span style="color:#a6e22e">b</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">inFrame</span> <span style="color:#f92672">&amp;&amp;</span> <span style="color:#a6e22e">byte_</span> <span style="color:#f92672">==</span> <span style="color:#a6e22e">HDLCFlag</span> {
</span></span><span style="display:flex;"><span>            <span style="color:#75715e">// Конец фрейма — отдаём пакет</span>
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">pkt</span> <span style="color:#f92672">:=</span> make([]<span style="color:#66d9ef">byte</span>, len(<span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span>))
</span></span><span style="display:flex;"><span>            copy(<span style="color:#a6e22e">pkt</span>, <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span>)
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">select</span> {
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">case</span> <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">packets</span> <span style="color:#f92672">&lt;-</span> <span style="color:#a6e22e">pkt</span>:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">default</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">dropped</span>.<span style="color:#a6e22e">Add</span>(<span style="color:#ae81ff">1</span>) <span style="color:#75715e">// канал переполнен — счётчик дропов</span>
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span> = <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span>[:<span style="color:#ae81ff">0</span>]
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">inFrame</span> = <span style="color:#66d9ef">false</span>
</span></span><span style="display:flex;"><span>        } <span style="color:#66d9ef">else</span> <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">byte_</span> <span style="color:#f92672">==</span> <span style="color:#a6e22e">HDLCFlag</span> {
</span></span><span style="display:flex;"><span>            <span style="color:#75715e">// Начало фрейма</span>
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">inFrame</span> = <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span> = <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span>[:<span style="color:#ae81ff">0</span>]
</span></span><span style="display:flex;"><span>        } <span style="color:#66d9ef">else</span> <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">inFrame</span> <span style="color:#f92672">&amp;&amp;</span> len(<span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span>) &lt; <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">hwMTU</span> {
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">byte_</span> <span style="color:#f92672">==</span> <span style="color:#a6e22e">HDLCEscape</span> {
</span></span><span style="display:flex;"><span>                <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">escape</span> = <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span>            } <span style="color:#66d9ef">else</span> {
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">escape</span> {
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">switch</span> <span style="color:#a6e22e">byte_</span> {
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">case</span> <span style="color:#a6e22e">HDLCFlag</span> ^ <span style="color:#a6e22e">HDLCEscMask</span>:
</span></span><span style="display:flex;"><span>                        <span style="color:#a6e22e">byte_</span> = <span style="color:#a6e22e">HDLCFlag</span>
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">case</span> <span style="color:#a6e22e">HDLCEscape</span> ^ <span style="color:#a6e22e">HDLCEscMask</span>:
</span></span><span style="display:flex;"><span>                        <span style="color:#a6e22e">byte_</span> = <span style="color:#a6e22e">HDLCEscape</span>
</span></span><span style="display:flex;"><span>                    }
</span></span><span style="display:flex;"><span>                    <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">escape</span> = <span style="color:#66d9ef">false</span>
</span></span><span style="display:flex;"><span>                }
</span></span><span style="display:flex;"><span>                <span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span> = append(<span style="color:#a6e22e">d</span>.<span style="color:#a6e22e">buf</span>, <span style="color:#a6e22e">byte_</span>)
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> len(<span style="color:#a6e22e">b</span>), <span style="color:#66d9ef">nil</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Три состояния: вне фрейма, внутри фрейма, ожидание escape. Логика точно воспроизводит
<code>readLoop</code> из <code>PipeInterface.py</code> — вплоть до того, что пустой фрейм <code>0x7E 0x7E</code> доставляет
пустой пакет (Python делает то же самое, вызывая <code>process_incoming(b&quot;&quot;)</code> без проверки длины).</p>
<p>Максимальный размер буфера ограничен <code>hwMTU</code> (по умолчанию 1064 байта, как в <code>PipeInterface.py#L72</code>).
Пакеты, которые не влезают в буферизованный канал, считаются дропнутыми — счётчик <code>DroppedPackets()</code>
помогает мониторить нагрузку.</p>
<h2 id="переподключение">Переподключение</h2>
<h3 id="два-режима">Два режима</h3>
<p>По умолчанию режим <strong>fixed delay</strong> — точно как <code>respawn_delay</code> в Python:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#66d9ef">func</span> (<span style="color:#a6e22e">r</span> <span style="color:#f92672">*</span><span style="color:#a6e22e">reconnector</span>) <span style="color:#a6e22e">backoff</span>(<span style="color:#a6e22e">attempt</span> <span style="color:#66d9ef">int</span>) <span style="color:#a6e22e">time</span>.<span style="color:#a6e22e">Duration</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">attempt</span> <span style="color:#f92672">==</span> <span style="color:#ae81ff">0</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#ae81ff">0</span> <span style="color:#75715e">// первая попытка без задержки</span>
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> !<span style="color:#a6e22e">r</span>.<span style="color:#a6e22e">exponentialBackoff</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">r</span>.<span style="color:#a6e22e">baseDelay</span> <span style="color:#75715e">// фиксированная задержка</span>
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// экспоненциальный бэкофф с джиттером ±25%, потолок 60s</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">exp</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">math</span>.<span style="color:#a6e22e">Pow</span>(<span style="color:#ae81ff">2</span>, float64(<span style="color:#a6e22e">attempt</span><span style="color:#f92672">-</span><span style="color:#ae81ff">1</span>))
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">delayF</span> <span style="color:#f92672">:=</span> float64(<span style="color:#a6e22e">r</span>.<span style="color:#a6e22e">baseDelay</span>) <span style="color:#f92672">*</span> <span style="color:#a6e22e">exp</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">delayF</span> &gt; float64(<span style="color:#ae81ff">60</span><span style="color:#f92672">*</span><span style="color:#a6e22e">time</span>.<span style="color:#a6e22e">Second</span>) {
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">delayF</span> = float64(<span style="color:#ae81ff">60</span> <span style="color:#f92672">*</span> <span style="color:#a6e22e">time</span>.<span style="color:#a6e22e">Second</span>)
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#a6e22e">time</span>.<span style="color:#a6e22e">Duration</span>(<span style="color:#a6e22e">delayF</span> <span style="color:#f92672">*</span> (<span style="color:#ae81ff">0.75</span> <span style="color:#f92672">+</span> <span style="color:#a6e22e">rand</span>.<span style="color:#a6e22e">Float64</span>()<span style="color:#f92672">*</span><span style="color:#ae81ff">0.5</span>))
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Для долгоживущих сервисов, которые сами управляют переподключением, есть <code>ExponentialBackoff: true</code>.</p>
<h3 id="errpipeclosed">ErrPipeClosed</h3>
<p>Есть нюанс: если Go-процесс запущен как дочерний rnsd, то при получении EOF на stdin он должен
завершиться, а не переподключаться — rnsd сам перезапустит процесс через <code>respawn_delay</code>.</p>
<p>Для этого есть <code>ExitOnEOF: true</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#a6e22e">iface</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">rnspipe</span>.<span style="color:#a6e22e">New</span>(<span style="color:#a6e22e">rnspipe</span>.<span style="color:#a6e22e">Config</span>{
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">ExitOnEOF</span>: <span style="color:#66d9ef">true</span>, <span style="color:#75715e">// вернуть ErrPipeClosed вместо переподключения</span>
</span></span><span style="display:flex;"><span>})
</span></span></code></pre></div><p>При <code>ExitOnEOF=true</code> и получении чистого EOF <code>Start</code> возвращает <code>ErrPipeClosed</code> немедленно, без
ожидания <code>ReconnectDelay</code>. Это позволяет процессу быстро завершиться и дать rnsd сигнал к
перезапуску.</p>
<h2 id="конкурентность">Конкурентность</h2>
<h3 id="syncrwmutex-для-состояния">sync.RWMutex для состояния</h3>
<p>Флаги состояния и коллбэки защищены <code>sync.RWMutex</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#66d9ef">type</span> <span style="color:#a6e22e">Interface</span> <span style="color:#66d9ef">struct</span> {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">mu</span>              <span style="color:#a6e22e">sync</span>.<span style="color:#a6e22e">RWMutex</span> <span style="color:#75715e">// защищает: pipeOnline, transportOnline, started, onSend, onStatus, cancelFn</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">writeMu</span>         <span style="color:#a6e22e">sync</span>.<span style="color:#a6e22e">Mutex</span>   <span style="color:#75715e">// сериализует запись в Stdout в Receive()</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">pipeOnline</span>      <span style="color:#66d9ef">bool</span>         <span style="color:#75715e">// pipe к rnsd жив (Start/readLoop)</span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">transportOnline</span> <span style="color:#66d9ef">bool</span>         <span style="color:#75715e">// сетевая сторона жива (SetOnline)</span>
</span></span><span style="display:flex;"><span>    <span style="color:#75715e">// ...</span>
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p><code>writeMu</code> отдельный — чтобы несколько горутин могли одновременно вызывать <code>Receive()</code> без
конфликтов на записи в stdout.</p>
<h3 id="атомарные-счётчики-метрик">Атомарные счётчики метрик</h3>
<p>Трафик считается без локов:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#a6e22e">packetsSent</span>     <span style="color:#a6e22e">atomic</span>.<span style="color:#a6e22e">Uint64</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">packetsReceived</span> <span style="color:#a6e22e">atomic</span>.<span style="color:#a6e22e">Uint64</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">bytesSent</span>       <span style="color:#a6e22e">atomic</span>.<span style="color:#a6e22e">Uint64</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">bytesReceived</span>   <span style="color:#a6e22e">atomic</span>.<span style="color:#a6e22e">Uint64</span>
</span></span></code></pre></div><p><code>atomic.Uint64</code> из стандартной библиотеки — безопасно читать из любого числа горутин
без <code>sync.Mutex</code>.</p>
<h3 id="жизненный-цикл-горутины">Жизненный цикл горутины</h3>
<p><code>readLoop</code> запускает одну горутину для <code>io.Copy</code>. При отмене контекста нужно разблокировать
её — если <code>Stdin</code> реализует <code>io.Closer</code>, закрываем его и ждём завершения:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#66d9ef">case</span> <span style="color:#f92672">&lt;-</span><span style="color:#a6e22e">ctx</span>.<span style="color:#a6e22e">Done</span>():
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">iface</span>.<span style="color:#a6e22e">config</span>.<span style="color:#a6e22e">Stdin</span> <span style="color:#f92672">!=</span> <span style="color:#a6e22e">os</span>.<span style="color:#a6e22e">Stdin</span> {
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> <span style="color:#a6e22e">closer</span>, <span style="color:#a6e22e">ok</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">iface</span>.<span style="color:#a6e22e">config</span>.<span style="color:#a6e22e">Stdin</span>.(<span style="color:#a6e22e">io</span>.<span style="color:#a6e22e">Closer</span>); <span style="color:#a6e22e">ok</span> {
</span></span><span style="display:flex;"><span>            <span style="color:#a6e22e">_</span> = <span style="color:#a6e22e">closer</span>.<span style="color:#a6e22e">Close</span>()
</span></span><span style="display:flex;"><span>            <span style="color:#f92672">&lt;-</span><span style="color:#a6e22e">readErr</span> <span style="color:#75715e">// ждём горутину</span>
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">nil</span>
</span></span></code></pre></div><p><code>os.Stdin</code> намеренно исключён из этого пути — его закрытие аффектирует весь процесс.
Если <code>Stdin</code> не реализует <code>io.Closer</code>, логируется предупреждение о возможной утечке горутины.</p>
<h2 id="нулевые-зависимости">Нулевые зависимости</h2>
<p>Основная библиотека использует только стандартную библиотеку Go — <code>go.sum</code> пуст для основного
модуля. Это сознательное решение:</p>
<p><strong>Плюсы:</strong></p>
<ul>
<li>Нет diamond-dependency проблем при вложении в другой проект</li>
<li>Сборка работает в любом окружении без <code>go get</code></li>
<li>Меньше поверхность для supply-chain атак</li>
</ul>
<p><strong>Минусы:</strong></p>
<ul>
<li>Нет <code>zerolog</code> или <code>zap</code> — логирование через стандартный <code>log/slog</code></li>
<li>Нет готового <code>backoff</code>-пакета — реализован вручную (невелик)</li>
</ul>
<p>Для parity-тестов нужен только <code>python3</code> в PATH — скрипт встроен прямо в тест как константа,
без сторонних Python-пакетов.</p>
<h2 id="тестирование">Тестирование</h2>
<h3 id="юнит-тесты">Юнит-тесты</h3>
<p>Базовые сценарии в <code>pipe_test.go</code>: encode/decode, byte stuffing, пустые пакеты, метрики,
горутинные гонки. Тесты с <code>sync.Mutex</code>/<code>atomic</code> прогоняются с детектором гонок:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>make test
</span></span><span style="display:flex;"><span><span style="color:#75715e"># эквивалентно: go test -race ./...</span>
</span></span></code></pre></div><h3 id="parity-тесты-с-python">Parity-тесты с Python</h3>
<p>Самый важный момент — убедиться, что наша HDLC-кодировка совместима с эталонной реализацией.
В <code>parity_test.go</code> (тег сборки <code>integration</code>) Go-кодировщик отправляет фреймы Python-скрипту,
который их декодирует:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#66d9ef">func</span> <span style="color:#a6e22e">TestHDLCParityPython</span>(<span style="color:#a6e22e">t</span> <span style="color:#f92672">*</span><span style="color:#a6e22e">testing</span>.<span style="color:#a6e22e">T</span>) {
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">payload</span> <span style="color:#f92672">:=</span> []byte(<span style="color:#e6db74">&#34;hello-parity-test&#34;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">enc</span> <span style="color:#f92672">:=</span> <span style="color:#f92672">&amp;</span><span style="color:#a6e22e">rnspipe</span>.<span style="color:#a6e22e">Encoder</span>{}
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">frame</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">enc</span>.<span style="color:#a6e22e">Encode</span>(<span style="color:#a6e22e">payload</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">cmd</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">exec</span>.<span style="color:#a6e22e">Command</span>(<span style="color:#a6e22e">python</span>, <span style="color:#a6e22e">tmp</span>.<span style="color:#a6e22e">Name</span>())
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">cmd</span>.<span style="color:#a6e22e">Stdin</span> = <span style="color:#a6e22e">bytes</span>.<span style="color:#a6e22e">NewReader</span>(<span style="color:#a6e22e">frame</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">_</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">cmd</span>.<span style="color:#a6e22e">Output</span>()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> !<span style="color:#a6e22e">bytes</span>.<span style="color:#a6e22e">Equal</span>(<span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">payload</span>) {
</span></span><span style="display:flex;"><span>        <span style="color:#a6e22e">t</span>.<span style="color:#a6e22e">Errorf</span>(<span style="color:#e6db74">&#34;Python decoded %q, want %q&#34;</span>, <span style="color:#a6e22e">out</span>, <span style="color:#a6e22e">payload</span>)
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>Есть и обратный тест: Python кодирует фрейм, Go декодирует. И полный round-trip:
Go → Python → Go с бинарными нагрузками (<code>0x7E</code>, <code>0x7D</code>, их комбинациями).</p>
<h2 id="планы">Планы</h2>
<p>Абстракция PipeInterface делает добавление новых транспортов почти механической задачей: реализуй
чтение/запись пакетов через выбранный канал, вызывай <code>SetOnline</code> по состоянию соединения — и всё.</p>
<p>Несколько транспортов в планах:</p>
<p><strong>Email-транспорт</strong> — туннелирование пакетов Reticulum через SMTP/IMAP. Каждый пакет упаковывается
в письмо, отправляется на почтовый сервер и считывается с другой стороны. Это позволяет обходить
цензуру в регионах, где основной интернет-трафик фильтруется, но email ещё работает.</p>
<p><strong>S3-транспорт</strong> — объектное хранилище (AWS S3, MinIO и аналоги) как асинхронное реле пакетов.
Пакеты записываются как объекты с известным именованием; получатель периодически читает их и удаляет.
Подходит для store-and-forward поверх сильно фильтруемых соединений или при асимметричном канале.</p>
<p>Общая идея: PipeInterface — это не только удобный способ написать транспорт для rnsd, но и
инструмент для реализации нестандартных каналов, которые никогда не войдут в официальный Python-код.</p>
<h2 id="итоги">Итоги</h2>
<p><strong>Что получилось:</strong></p>
<ul>
<li>Совместимая с Python реализация PipeInterface протокола на чистом Go</li>
<li>Два режима переподключения (fixed delay и exponential backoff с jitter)</li>
<li>Потоковый HDLC-декодер как <code>io.Writer</code> — подключается к <code>io.Copy</code> напрямую</li>
<li>Метрики через атомарные счётчики</li>
<li>Двухбитное состояние online: <code>pipeOnline</code> (rnsd-сторона) + <code>transportOnline</code> (сетевая сторона)</li>
</ul>
<p><strong>Что узнал:</strong></p>
<p>Точное воспроизведение behaviour другой реализации — это отдельная задача. Казалось бы, простой
протокол: флаговый байт, escape-последовательности. Но детали вроде «пустой фрейм тоже доставляется»
или «порядок escape ESC до FLAG» обнаруживаются только при внимательном чтении кода и parity-тестах.</p>
<p>Изоляция через <code>ExitOnEOF</code>/<code>ErrPipeClosed</code> для режима дочернего процесса тоже не очевидна сразу —
нужно понять жизненный цикл rnsd, прежде чем сделать правильный интерфейс.</p>
<p><strong>Состояние проекта:</strong></p>
<p>Библиотека стабильна, покрыта тестами, используется в моих проектах. Исходный код на GitHub:
<a href="https://github.com/x3ps/go-rns-pipe">x3ps/go-rns-pipe</a>. Версия — v0.1.1. Лицензия — MIT.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>go get github.com/x3ps/go-rns-pipe@v0.1.1
</span></span></code></pre></div>]]></content></item></channel></rss>